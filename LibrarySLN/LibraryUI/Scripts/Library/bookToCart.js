'use strict';
$(document).ready(function () {
    (function () {
              
        if ($('#registerResult').val() === 'True') {

            $('.setToBasket').prop('disabled', false);
        }

        var userCart = [];

        $('.setToBasket').click(function () {

            alert('This feature is in progress.\n To get a book:\n >>> Librarian >>> BookCards >>> Getout Book.');
        })

    })();
});